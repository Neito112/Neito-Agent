# Vision pipeline test

The intended order is local-first: YOLO11n/custom model, local protocol answers, then YOLO-World and Phidata only for an unknown situation. The repository currently contains situation metadata such as `datasets/lol/baron_stolen/situation_meta.json`; it does not yet ship a fine-tuned YOLO11n weights file for every protocol.
